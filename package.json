{
  "name": "saju-offline-v2",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "node scripts/sync-rws-assets.js",
    "dev": "vite",
    "prebuild": "node scripts/sync-rws-assets.js",
    "build": "vite build",
    "prepreview": "node scripts/sync-rws-assets.js",
    "preview": "vite preview",
    "test": "node --test tests/*.test.js",
    "check:network": "node scripts/check-network.js",
    "check": "npm test && npm run check:network && npm run build"
  },
  "dependencies": {
    "manseryeok": "2.0.0"
  },
  "devDependencies": {
    "vite": "^7.1.0"
  }
}
